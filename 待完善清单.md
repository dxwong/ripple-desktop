# Ripple Agent 待完善清单

> 🕐 最后更新: 2026-05-18

## 🔴 高优先级（影响核心功能）

### 1. ~~文件树功能~~
- ~~**当前**：右侧面板文件树，支持展开/折叠目录，双击文件在预览面板查看内容（支持文本文件和图片预览）~~
- ~~**问题**：双击打开文件读取失败，JSON 解析错误~~
- **状态**：✅ 已修复 (`88fdd81` + `04c68f2`)，支持文件树浏览和预览
- **待完善**：右键菜单、文件图标优化

### 2. ~~工具执行结果展示~~
- **当前**：✅ 已完成 ToolCallCard 组件 (`88fdd81`)，展示工具名称、参数、执行结果
- **期望**：统一的 ToolCallCard 组件，展示工具名称、参数、执行结果
- **参考**：Claude Code 的工具执行折叠卡片

## 🟡 中优先级（提升体验）

### 3. ~~代码编辑增强~~ ✅ 已完成
- ~~**EditBlock 支持**：后端已实现 `parseEditBlocks`/`applyEditBlocks`，前端未集成~~
- ~~**Diff 预览**：编辑文件前展示 diff，确认后再应用~~
- **状态**：✅ 已集成 (`324a5bb`)
  - 新增 `useEditBlockDetector.ts` Hook 检测 EditBlock 格式
  - 新增 `EditBlockPreview.tsx` 组件集成 DiffPreview
  - ChatMessage 自动显示 diff 预览，支持应用/忽略
- **Checkpoint 集成**：✅ 已完成 (`557eeda` + `fe38c81`)
  - 后端 API：`GET/POST /api/checkpoints`、`POST /api/checkpoints/:id/restore`、`GET /api/checkpoints/:id/diff`
  - 前端 UI：`CheckpointPanel` 组件（快照列表、创建、回滚、删除、差异预览）

### 4. ~~AI 停止按钮~~ ✅ 已完成
- ~~**当前**：流式输出期间无法停止~~
- **状态**：✅ 已实现 (`a395e93`)
  - MessageInput 动态显示停止/发送按钮
  - 调用 `stopStreaming` 中断 SSE 请求
  - 快捷键支持 (Esc)

### 5. Lint/Test 集成
- **当前**：后端有 `Linter`/`TestRunner` 工具，未注册到 Agent
- **期望**：编辑后自动运行 Linter，错误反馈给 Agent 自修复

### 5. 沙盒执行
- **当前**：`shell` 命令直接执行，无隔离
- **方案对比**：
  - Docker 容器（最安全，需 Docker 环境）
  - WSL 沙盒（Windows 专属）
  - 维持现状 + 确认制（当前方案）

## 🟢 低优先级（锦上添花）

### 6. 会话持久化
- **当前**：前端内存存储，刷新页面丢失
- **期望**：与后端 `JsonlSessionStorage` 对接，实现会话持久化

### 7. 模型配置优化
- **当前**：每个模型需单独配置 endpoint/apiKey
- **期望**：预设常用服务商（DeepSeek、OpenAI），一键切换

### 8. 快捷键支持
- 新建对话 (Ctrl+N)
- 切换对话 (Ctrl+Tab)
- 停止生成 (Ctrl+C / Esc)
- 打开设置 (Ctrl+,)

### 9. 主题/外观
- 代码高亮主题切换
- 字体大小调节
- 紧凑/舒适模式

---

## ✅ 已完成功能

- [x] 项目与工作目录绑定
- [x] 文件操作 AgentTool（read/write/list/remove/shell）
- [x] 操作确认机制（横幅提示）
- [x] 工作目录隔离（cwd 限制）
- [x] 流式响应（text/thinking/tool 事件）
- [x] 对话切换状态清理
- [x] 多模型配置管理
- [x] 文件树浏览（右侧面板）
- [x] 文件预览（文本文件和图片）
- [x] 操作权限设置（三种模式切换）
- [x] ToolCallCard 组件（工具执行状态展示）
- [x] 文件预览布局修复（关闭按钮 + 响应式宽度）
- [x] EditBlock 检测与 DiffPreview 集成
- [x] AI 停止按钮（支持中断流式输出）
- [x] Checkpoint 快照系统（创建、回滚、删除、差异预览）

---

## 🐛 已知问题

### 🔴 高优先级

| 问题 | 说明 | 状态 |
|------|------|------|
| ~~文件树双击打开读取失败~~ | ~~JSON 解析错误，无法读取文件内容~~ | ✅ 已修复 |

### 🟡 中优先级

| 问题 | 说明 | 状态 |
|------|------|------|
| （暂无） | - | - |

---

## 🎯 当前可用性评估

### 可以做的 ✅
- 使用 **DeepSeek** 模型进行编程开发
- 读取项目文件、列出目录
- 写入文件（需确认）
- 执行 shell 命令（需确认）
- 多轮对话上下文
- 三种权限模式切换（Auto/确认/只读）
- 文件树浏览和双击预览文件
- 工具执行状态展示（ToolCallCard）

### 不能做的 ❌
- 自动 lint/test（Linter/TestRunner 工具未注册到 Agent）

### 建议 🔧
当前软件 **可以用于编程**，且体验已大幅提升：
1. ✅ **EditBlock 差异预览** - AI 编辑文件前可预览 diff，确认后再应用
2. ✅ **停止按钮** - 流式输出期间可随时中断
3. 使用 DeepSeek 模型
4. 重要操作前点击 GitBranch 按钮创建快照（支持一键回滚）
5. 小步迭代，每次让 Agent 做单一任务

