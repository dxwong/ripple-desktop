# Ripple Agent 待完善清单

## 🔴 高优先级（影响核心功能）

### 1. 火山引擎 API 兼容性
- **问题**：火山引擎 `ark-code-latest` 调用后无响应，Agent 卡住
- **日志特征**：`agent.prompt()` 调用后没有 `agent_end` 事件
- **可能原因**：火山引擎的流式响应格式与 OpenAI 不完全兼容
- **建议**：后端排查 `openai-completions.ts` 对火山引擎响应的解析逻辑

### 2. 工具执行结果展示
- **当前**：Agent 自行描述工具执行结果，格式不统一
- **期望**：统一的 ToolCallCard 组件，展示工具名称、参数、执行结果
- **参考**：Claude Code 的工具执行折叠卡片

### 3. 文件树浏览
- **当前**：只能看到项目路径，无法浏览文件结构
- **期望**：侧边栏或独立面板展示项目目录树
- **功能**：点击文件查看内容、右键操作（复制路径、在编辑器中打开等）

## 🟡 中优先级（提升体验）

### 4. 操作权限设置
- **当前**：所有高风险操作都需要确认
- **期望**：设置面板增加权限选项
  - "默认允许"（信任模式）
  - "每次确认"（当前模式）
  - "只读模式"（禁止写操作）

### 5. 代码编辑增强
- **EditBlock 支持**：后端已实现 `parseEditBlocks`/`applyEditBlocks`，前端未集成
- **Diff 预览**：编辑文件前展示 diff，确认后再应用
- **Checkpoint 集成**：编辑前自动快照，支持一键回滚

### 6. Lint/Test 集成
- **当前**：后端有 `Linter`/`TestRunner` 工具，未注册到 Agent
- **期望**：编辑后自动运行 Linter，错误反馈给 Agent 自修复

### 7. 沙盒执行
- **当前**：`shell` 命令直接执行，无隔离
- **方案对比**：
  - Docker 容器（最安全，需 Docker 环境）
  - WSL 沙盒（Windows 专属）
  - 维持现状 + 确认制（当前方案）

## 🟢 低优先级（锦上添花）

### 8. 会话持久化
- **当前**：前端内存存储，刷新页面丢失
- **期望**：与后端 `JsonlSessionStorage` 对接，实现会话持久化

### 9. 模型配置优化
- **当前**：每个模型需单独配置 endpoint/apiKey
- **期望**：预设常用服务商（DeepSeek、OpenAI、火山等），一键切换

### 10. 快捷键支持
- 新建对话 (Ctrl+N)
- 切换对话 (Ctrl+Tab)
- 停止生成 (Ctrl+C / Esc)
- 打开设置 (Ctrl+,)

### 11. 主题/外观
- 代码高亮主题切换
- 字体大小调节
- 紧凑/舒适模式

---

## ✅ 已完成功能

- [x] 项目与工作目录绑定
- [x] 文件操作 AgentTool（read/write/list/remove/shell）
- [x] 操作确认机制（横幅提示）
- [x] 工作目录隔离（cwd 限制）
- [x] 流式响应（text/thinking/tool 事件）
- [x] 对话切换状态清理
- [x] 多模型配置管理

---

## 🎯 当前可用性评估

### 可以做的 ✅
- 使用 **DeepSeek** 模型进行编程开发
- 读取项目文件、列出目录
- 写入文件（需确认）
- 执行 shell 命令（需确认）
- 多轮对话上下文

### 不能做的 ❌
- 使用 **火山引擎** 模型（卡死问题）
- 可视化浏览文件树
- 代码 diff 预览
- 一键回滚编辑
- 自动 lint/test

### 建议 🔧
当前软件 **可以用于编程**，但建议：
1. 使用 DeepSeek 模型（火山引擎等有兼容性问题）
2. 重要操作前手动备份（无 checkpoint 回滚）
3. 小步迭代，每次让 Agent 做单一任务

