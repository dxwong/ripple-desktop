# Ripple Desktop — 开发进展与交接文档

> 最后更新: 2026-05-17
> 当前分支: `dev`
> 最新提交: `ff992f5` — fix: Rust 编译错误

---

## 一、项目概述

### 1.1 项目定位

Ripple Desktop 是一个 **AI 编程助手桌面端**，采用 **纯 TUI 架构**（前端仅作为界面层，所有 AI 能力由后端提供）。

- **前端**: Tauri 2.x + React 18 + TypeScript + Tailwind CSS
- **后端**: Ripple-Agent（独立项目，位于 `../pi-mono/ripple-agent`）
- **产品对标**: Claude Desktop

### 1.2 架构原则

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   Tauri 前端     │────→│  Ripple-Agent   │────→│   LLM APIs      │
│   (纯UI展示)     │     │  (业务逻辑中心)  │     │  (OpenAI等)     │
└─────────────────┘     └─────────────────┘     └─────────────────┘
        ↑                                               ↓
        └───────────────────────────────────────────────┘
             所有 AI 能力必须通过后端，前端不直连 API
```

### 1.3 项目结构

```
ripple-desktop-Tauri/
├── src/                      # 前端源码（React + TypeScript）
│   ├── components/
│   │   ├── ChatMessage.tsx   # 消息气泡（流式/完成态）
│   │   ├── ChatView.tsx      # 主聊天视图（含窗口控制）
│   │   ├── CodeEditor.tsx    # Monaco 代码编辑器
│   │   ├── MessageInput.tsx  # 消息输入框（模型选择器）
│   │   ├── SettingsPanel.tsx # 设置面板（多模型配置管理）
│   │   ├── Sidebar.tsx       # 侧边栏（项目+对话列表）
│   │   ├── LogPanel.tsx      # 底部日志面板（新增）
│   │   └── ErrorBoundary.tsx # 全局错误边界
│   ├── hooks/
│   │   ├── useSettings.ts    # 设置管理
│   │   ├── useStore.ts       # 统一存储层
│   │   ├── useStreamingChat.ts # 流式对话管理（核心）
│   │   ├── useProjects.ts    # 项目管理
│   │   ├── useFolderPicker.ts # 文件夹选择器
│   │   └── useTauri.ts       # Tauri 环境检测
│   ├── services/
│   │   ├── api.ts            # HTTP API 客户端（调用后端）
│   │   └── sse.ts            # SSE 流式客户端
│   ├── types/
│   │   └── index.ts          # TypeScript 类型定义
│   ├── styles/
│   │   └── globals.css
│   ├── App.tsx
│   └── main.tsx
├── src-tauri/                # Tauri Rust 后端
│   ├── src/
│   │   ├── lib.rs            # IPC 命令 + 后端进程管理
│   │   └── main.rs
│   └── tauri.conf.json
├── doc/                      # 开发文档
│   ├── DEVELOPMENT-01.md     # 开发进展记录
│   ├── 经验教训.md           # 踩坑记录
│   └── 开发计划-前端对接ripple-agent.md
├── .gitignore
├── package.json
├── tailwind.config.js
└── vite.config.ts
```

---

## 二、当前开发状态

### 2.1 已完成的工作

| 阶段 | 内容 | 提交 |
|------|------|------|
| 清理 | 移除所有 OpenCode 和桥接相关代码 | `34f92fa` |
| 清理 | 移除桥接服务（useBridge、ws_client、bridge/目录） | `34f92fa` |
| Phase 1 | 新建 HTTP/SSE 通信层（api.ts + sse.ts） | `4a80ec5` |
| Phase 1 | 重构 useStreamingChat 对接后端 API | `4a80ec5` |
| Phase 1 | 启动时自动检测后端连接 | `4a80ec5` |
| 直连 | ~~客户端 API 直连模式（已废弃）~~ | `6691c1d` |
| 重构 | 纯 TUI 模式重构（删除客户端直连） | `71ce482` |
| 重构 | API 测试改为走后端 `/api/test-connection` | `71ce482` |
| 重构 | 新增底部日志面板 LogPanel | `71ce482` |
| 重构 | Tauri 自动启动 Node 子进程 | `71ce482` |
| 修复 | Rust 编译错误修复 | `ff992f5` |

### 2.2 当前架构模式

**三种运行模式（自动切换）**：

```
Tauri桌面模式:  启动App → 自动启动Node后端 → 等待2秒 → 检测连接 → 就绪
浏览器开发模式:  手动启动后端 → 启动npm run dev → 检测连接 → 就绪
后端未连接:      显示黄色"模拟"标签，使用模拟数据（仅开发测试）
```

### 2.3 待完成的工作

详见 `doc/开发计划-前端对接ripple-agent.md`，主要剩余：

- **Phase 1.5-1.8**: 会话管理 API 适配、模型列表获取、ThinkingBlock/ToolCallCard 组件
- **Phase 2**: UI 增强（停止生成、时间分组、重命名、快捷键）
- **Phase 3**: 高级功能（文件上传、Artifacts 分屏）
- **Phase 4**: 清理优化（移除 Python Bridge 残留、简化 Rust 后端）

---

## 三、关键技术决策

### 3.1 通信协议

| 场景 | 协议 | 端点 |
|------|------|------|
| 健康检查 | HTTP GET | `/api/health` |
| 模型列表 | HTTP GET | `/api/models` |
| 会话管理 | HTTP GET/POST/DELETE | `/api/sessions/*` |
| 流式聊天 | HTTP POST + SSE | `/api/chat` |
| API 测试 | HTTP POST | `/api/test-connection` |

### 3.2 SSE 事件格式

后端 `/api/chat` 返回的 SSE 事件：

```
data: {"type":"text","text":"Hello"}
data: {"type":"thinking","text":"思考中..."}
data: {"type":"tool-start","name":"read_file"}
data: {"type":"tool-end","name":"read_file"}
data: {"type":"done"}
data: {"type":"error","error":"错误信息"}
```

### 3.3 后端启动

- **Tauri 环境**: Rust 通过 `Command::new("node")` 启动子进程
- **浏览器环境**: 用户手动启动 `node packages/server/dist/index.js`
- **后端路径查找**: 优先从配置读取，默认查找 `../pi-mono/ripple-agent`

### 3.4 配置管理

- 模型配置（API Key、Endpoint、Model）在前端设置面板配置
- 通过 `save_config`/`load_config` IPC 命令持久化到本地 JSON
- 后端不存储 API Key，由前端在聊天时传给后端

---

## 四、后端依赖

### 4.1 后端需要提供的 API

| 端点 | 方法 | 说明 | 状态 |
|------|------|------|------|
| `/api/health` | GET | 健康检查 | ✅ 已有 |
| `/api/models` | GET | 模型列表 | ✅ 已有 |
| `/api/sessions` | GET | 会话列表 | ✅ 已有 |
| `/api/sessions/:id` | GET | 会话详情 | ✅ 已有 |
| `/api/sessions/:id` | POST | 创建/更新会话 | ✅ 已有 |
| `/api/sessions/:id` | DELETE | 删除会话 | ✅ 已有 |
| `/api/chat` | POST | SSE 流式聊天 | ✅ 已有 |
| `/api/reset` | POST | 重置会话 | ✅ 已有 |
| `/api/test-connection` | POST | API 连接测试 | ✅ 已添加 |
| `/api/stats/summary` | GET | 统计摘要 | ✅ 已有 |
| `/api/stats/records` | GET | 统计记录 | ✅ 已有 |

### 4.2 后端启动命令

```bash
cd ../pi-mono/ripple-agent
node packages/server/dist/index.js
```

> ⚠️ 注意：必须在 ripple-agent 根目录执行，否则 dotenv 找不到 `.env` 文件

### 4.3 后端 CORS

后端已配置 CORS 中间件，允许前端跨域访问。

---

## 五、如何运行

### 5.1 开发模式（浏览器）

```bash
# 终端 1: 启动后端
cd ../pi-mono/ripple-agent
node packages/server/dist/index.js

# 终端 2: 启动前端
cd ripple-desktop-Tauri
npm run dev
# 访问 http://localhost:1420
```

### 5.2 开发模式（Tauri 桌面）

```bash
npm run tauri:dev
# 会自动启动 Vite + Rust 编译 + 自动启动后端
```

### 5.3 构建打包

```bash
npm run tauri:build
```

---

## 六、关键文件说明

### 6.1 前端核心文件

| 文件 | 说明 | 注意事项 |
|------|------|---------|
| `src/App.tsx` | 应用入口，启动检测后端连接 | 新增了 Tauri 自动启动后端逻辑 |
| `src/hooks/useStreamingChat.ts` | 流式对话核心 Hook | 支持 backend/simulate 两种模式 |
| `src/services/api.ts` | HTTP API 客户端 | 所有后端调用都通过此文件 |
| `src/services/sse.ts` | SSE 流式客户端 | SSEClient 类，支持超时和取消 |
| `src/components/LogPanel.tsx` | 底部日志面板 | 全局 Logger 单例，支持 info/warn/error/success |
| `src/components/SettingsPanel.tsx` | 设置面板 | API 测试按钮调用后端 `/api/test-connection` |

### 6.2 Rust 后端文件

| 文件 | 说明 | 注意事项 |
|------|------|---------|
| `src-tauri/src/lib.rs` | IPC 命令 + 后端进程管理 | 新增 start_backend/stop_backend 命令 |
| `src-tauri/Cargo.toml` | Rust 依赖 | 已移除 tokio/tungstenite 等桥接依赖 |

### 6.3 文档文件

| 文件 | 说明 |
|------|------|
| `doc/开发计划-前端对接ripple-agent.md` | 完整开发计划（4 个 Phase） |
| `doc/DEVELOPMENT-01.md` | 开发进展记录 |
| `doc/经验教训.md` | 开发踩坑记录（必读） |

---

## 七、常见问题

### Q: 启动后显示"模拟"而不是"已连接"？

A: 后端未启动或未运行。检查：
1. 后端是否已执行 `node packages/server/dist/index.js`
2. 后端端口是否为 3002
3. 浏览器控制台是否有 CORS 错误

### Q: Tauri 编译报错？

A: 确保 Rust 工具链 >= 1.77，且安装了 Windows SDK。

### Q: 如何添加新的后端 API 调用？

A: 在 `src/services/api.ts` 中添加新函数，使用 `request<T>()` 封装。

### Q: 日志面板在哪里？

A: 应用底部有一个可折叠的日志面板，显示后端连接状态、API 测试结果等。点击展开可查看详细日志。

---

## 八、Git 提交规范

参考 `git使用规则-最高级别.md`：

- 分支策略：`main`（稳定）← `dev`（开发）
- 提交格式：`type: 描述`
- 当前所有开发在 `dev` 分支进行