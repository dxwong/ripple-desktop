# Ripple Desktop — 开发进展与交接文档

> 🕐 最后更新: 2026-05-17
> 🌿 当前分支: `dev`（前端 + 后端均在 dev 分支开发）
> 📦 前端仓库: `ripple-desktop-Tauri` → `origin/dev`
> 📦 后端仓库: `pi-mono/ripple-agent` → `origin/dev`

---

## 一、项目概述

### 1.1 定位

Ripple Desktop 是一个 **AI 编程助手桌面端**，对标 **Claude Code/Desktop**。采用 **纯 TUI 架构**（前端仅作为界面层，所有 AI 能力由后端提供）。

- **前端**: Tauri 2.x + React 18 + TypeScript + Tailwind CSS
- **后端**: Ripple-Agent（独立项目，位于 `../pi-mono/ripple-agent`）
- **通信**: HTTP REST + SSE 流式

### 1.2 架构

```
┌─────────────────┐     ┌─────────────────────┐     ┌─────────────────┐
│   Tauri 前端     │────→│  Ripple-Agent       │────→│   LLM APIs      │
│   (纯UI展示)     │     │  (业务逻辑中心)      │     │  (DeepSeek等)   │
│   React SPA      │     │  Agent Loop + Tools │     │  兼容OpenAI     │
└─────────────────┘     └─────────────────────┘     └─────────────────┘
        ↑                      ↗                       OpenAI/火山引擎
        └── Tauri IPC ─────────┘
           启动/停止后端进程
```

### 1.3 项目结构

```
ripple-desktop-Tauri/
├── src/                      # 前端源码（React + TypeScript）
│   ├── components/
│   │   ├── ChatMessage.tsx       # 消息气泡（流式/完成态）
│   │   ├── ChatView.tsx          # 主聊天视图（窗口控制 + 消息列表）
│   │   ├── CodeEditor.tsx        # Monaco 代码编辑器
│   │   ├── ErrorBoundary.tsx     # 全局错误边界
│   │   ├── LogPanel.tsx          # 底部日志面板
│   │   ├── MessageInput.tsx      # 消息输入框（模型选择器）
│   │   ├── SettingsPanel.tsx     # 设置面板（多模型配置管理）
│   │   ├── Sidebar.tsx           # 侧边栏（项目 + 对话列表）
│   │   ├── Titlebar.tsx          # Tauri 窗口控制按钮
│   │   └── ToolConfirmBanner.tsx # 工具执行确认横幅（新增）
│   ├── hooks/
│   │   ├── useSettings.ts        # 设置管理 + 持久化
│   │   ├── useStore.ts           # 统一存储层
│   │   ├── useStreamingChat.ts   # 流式对话管理（核心 Hook）
│   │   ├── useProjects.ts        # 项目管理
│   │   ├── useFolderPicker.ts    # 文件夹选择器
│   │   └── useTauri.ts           # Tauri 环境检测
│   ├── services/
│   │   ├── api.ts                # HTTP API 客户端（所有后端调用）
│   │   └── sse.ts                # SSE 流式客户端
│   ├── types/
│   │   └── index.ts              # TypeScript 类型定义
│   ├── styles/
│   │   └── globals.css           # Tailwind 全局样式
│   ├── App.tsx
│   └── main.tsx
├── src-tauri/                # Tauri Rust 后端
│   ├── src/
│   │   ├── lib.rs                # IPC 命令 + 后端进程管理
│   │   └── main.rs
│   └── tauri.conf.json
├── doc/                      # 开发文档
│   ├── DEVELOPMENT-01.md
│   ├── 经验教训.md
│   └── 开发计划-前端对接ripple-agent.md
├── package.json
├── tailwind.config.js
└── vite.config.ts
```

---

## 二、当前开发状态（2026-05-17）

### 2.1 已完成的功能

| 阶段 | 功能 | 前端提交 | 后端提交 |
|------|------|---------|---------|
| 架构 | 移除所有 OpenCode/桥接代码，纯 TUI 架构 | `34f92fa` | - |
| 通信 | HTTP/SSE 通信层（api.ts + sse.ts） | `4a80ec5` | - |
| 通信 | SSE 事件：text/thinking/tool-start/tool-end/tool-request/done/error | `fef369c` | - |
| 后端 | 动态模型配置（前端传 endpoint/apiKey/model） | - | `1848ea4` |
| 后端 | CORS 支持 | - | `8c58c6d` |
| 后端 | 工具执行确认机制（confirmRequired） | - | `6aca776` |
| 后端 | 工作目录隔离（cwd 限制 + resolveSafePath） | - | `553963e` |
| 后端 | endpoint URL 清理（逗号/空格/斜杠） | - | `6b16293` |
| 后端 | 拒绝工具后 AI 不再假装成功 | - | `6fa62c6` |
| 前端 | 流式对话管理（useStreamingChat） | `4a80ec5` | - |
| 前端 | 工作目录(cwd)传递给后端 | `8d2a420` | - |
| 前端 | tool-request SSE 事件 + 确认 API | `fef369c` | - |
| 前端 | 工具执行确认横幅（非侵入式） | `57f1dae` | - |
| 前端 | 对话切换自动清理加载状态 | `57f1dae` | - |
| 前端 | Auto 确认按钮（输入框上方靠右） | `664205d` | - |
| 前端 | 工具提示左边竖线视觉 + emoji 图标 | `664205d` | - |
| 前端 | 拒绝时显示 ❌ 图标（非 ✅） | `ecc86a2` | - |
| 前端 | 模型配置保存时自动 trim | `97c6c18` | - |
| 前端 | 日志面板（Logger 单例 + 复制/清空） | `62fad93` | - |
| 前端 | 思考内容流式显示 + 自动展开 | `019f648` | - |
| 前端 | Tauri 自动启动后端 + 端口冲突清理 | `3f67a11` | - |
| 前端 | 设置面板（多模型配置管理） | `71ce482` | - |
| 前端 | 错误边界 ErrorBoundary | `c2c8274` | - |
| 前端 | 项目工作目录绑定 | `8d2a420` | - |

### 2.2 当前架构模式

**三种运行模式（自动切换）**：

```
Tauri桌面模式: 启动App → Rust自动启动Node后端 → 等待2秒 → 检测连接 → 就绪
浏览器开发模式: 手动启动后端 → npm run dev → 检测连接 → 就绪
后端未连接:     显示黄色"模拟"标签，使用模拟数据（仅开发测试）
```

### 2.3 SSE 事件格式

后端 `/api/chat/:sessionId` 返回的 SSE 事件：

```
data: {"type":"text","text":"Hello"}
data: {"type":"thinking","text":"增量思考文本"}
data: {"type":"tool-request","toolCallId":"xxx","toolName":"write_file","args":{...},"description":"...","riskLevel":"medium"}
data: {"type":"tool-start","name":"write_file"}
data: {"type":"tool-end","name":"write_file","isError":false}
data: {"type":"done"}
data: {"type":"error","error":"错误信息"}
```

### 2.4 工具执行确认流程

```
用户发送消息 → AI 返回 tool_call
→ Agent 需要确认（confirmRequired: true）
→ 后端发出 tool-request SSE 事件
→ 前端弹出确认横幅（输入框上方靠右）
→ 用户点击 允许/拒绝
→ 前端 POST /api/chat/:sessionId/confirm { toolCallId, approved }
→ 后端执行/跳过工具
→ 结果发给 AI → AI 继续生成回复
```

### 2.5 Auto 确认模式

- 输入框上方靠右有一个小按钮 `● auto`
- **灰色** = 手动确认模式（每次弹窗）
- **橙色发光** = Auto 模式（工具自动执行，不弹窗）
- 弹窗出现时 auto 按钮自动隐藏

---

## 三、后端 API 一览

| 端点 | 方法 | 说明 | 状态 |
|------|------|------|------|
| `/api/health` | GET | 健康检查 | ✅ |
| `/api/models` | GET | 模型列表 | ✅ |
| `/api/sessions` | GET | 会话列表 | ✅ |
| `/api/sessions/:id` | GET | 会话详情 | ✅ |
| `/api/sessions/:id` | POST | 创建/更新会话 | ✅ |
| `/api/sessions/:id` | DELETE | 删除会话 | ✅ |
| `/api/chat/:sessionId` | POST | SSE 流式聊天（需传 model, endpoint, apiKey, cwd） | ✅ |
| `/api/chat/:sessionId/confirm` | POST | 工具执行确认 | ✅ |
| `/api/reset` | POST | 重置会话 | ✅ |
| `/api/test-connection` | POST | API 连接测试 | ✅ |

### 聊天请求体格式

```json
{
  "model": "deepseek-v4-flash",
  "endpoint": "https://api.deepseek.com",
  "apiKey": "sk-xxx",
  "message": "新建一个文件...",
  "cwd": "C:\\Users\\xxx\\Documents\\project"
}
```

### 工具确认请求体

```json
{
  "toolCallId": "xxx",
  "approved": true,
  "reason": "用户允许",
  "modelId": "deepseek-v4-flash"
}
```

---

## 四、后端关键实现

后端位于 `../pi-mono/ripple-agent`，核心在 `packages/agent/src/`：

| 文件 | 作用 |
|------|------|
| `agent.ts` | Agent 主类，管理状态/事件/消息转换 |
| `agent-loop.ts` | Agent 主循环，执行工具调用（顺序/并行） |
| `harness/enhanced-agent-harness.ts` | Harness：确认等待、工具注册、SSE 事件桥接 |
| `harness/env/nodejs.ts` | NodeExecutionEnv：文件/Shell 操作（spawn） |
| `tools/` | 各种 AgentTool 定义（write_file / read_file / shell / list_dir / remove / create_dir） |
| `server/src/index.ts` | Express 服务器：路由 + SSE 转发 |

### 工具列表

| 工具 | 来源 | 需要确认 | 风险级别 |
|------|------|---------|---------|
| `write_file` | NodeExecutionEnv | ✅ | `medium` |
| `read_file` | NodeExecutionEnv | ✅ | `medium` |
| `shell` | NodeExecutionEnv | ✅ | `high` |
| `list_dir` | NodeExecutionEnv | ❌ | `low` |
| `remove` | NodeExecutionEnv | ✅ | `high` |
| `create_dir` | NodeExecutionEnv | ❌ | `low` |

---

## 五、如何运行

### 5.1 开发模式（浏览器）

```bash
# 终端 1: 启动后端
cd ../pi-mono/ripple-agent
node packages/server/dist/index.js
# 输出: Server started: http://localhost:3002

# 终端 2: 启动前端
cd ripple-desktop-Tauri
npm run dev
# 访问 http://localhost:1420
```

### 5.2 开发模式（Tauri 桌面）

```bash
npm run tauri:dev
# 自动启动 Vite + Rust 编译 + 自动启动后端
```

### 5.3 构建打包

```bash
npm run tauri:build
```

### 5.4 后端编译

```bash
cd ../pi-mono/ripple-agent
npm install
cd packages/server
npx tsc -p tsconfig.build.json
```

> ⚠️ 必须在 `ripple-agent` 根目录或 `packages/server` 目录执行，dotenv 需要加载 `.env`

---

## 六、关键文件说明

### 6.1 前端核心文件

| 文件 | 说明 | 注意事项 |
|------|------|---------|
| `src/App.tsx` | 应用入口，启动检测后端连接 | Tauri 环境自动启动后端子进程 |
| `src/hooks/useStreamingChat.ts` | 流式对话核心 Hook | 最复杂的 Hook，含 SSE 连接/消息拼接/工具确认/auto 模式 |
| `src/services/api.ts` | HTTP API 客户端 | 所有后端调用都通过此文件 |
| `src/services/sse.ts` | SSE 流式客户端 | SSEClient 类，支持超时/取消/重连 |
| `src/components/ChatView.tsx` | 主聊天视图 | 集成消息列表 + 输入框 + 确认横幅 + Auto 按钮 |
| `src/components/ToolConfirmBanner.tsx` | 工具确认横幅 | 非侵入式，可展开详情，带风险级别颜色 |
| `src/components/LogPanel.tsx` | 底部日志面板 | Logger 单例，info/warn/error/success |
| `src/components/SettingsPanel.tsx` | 设置面板 | 多模型配置管理，API 测试走后端 |

### 6.2 Rust 后端文件

| 文件 | 说明 | 注意事项 |
|------|------|---------|
| `src-tauri/src/lib.rs` | IPC 命令 + 后端进程管理 | start_backend/stop_backend 命令 |
| `src-tauri/Cargo.toml` | Rust 依赖 | 已移除桥接依赖 |

---

## 七、已知问题

### 🔴 高优先级

| 问题 | 说明 | 状态 |
|------|------|------|
| 火山引擎 API 兼容性 | `ark-code-latest` 调用后无响应，`agent.prompt()` 无 `agent_end` | ⏳ 待排查 |
| 文件回滚（Checkpoint） | 后端有 Checkpoint 功能，前端未集成 | ⏳ 未集成到前端 |
|  EditBlock / Diff 预览 | 后端的 parseEditBlocks / applyEditBlocks | ⏳ 未集成到前端 |

### 🟡 中优先级

| 功能 | 说明 | 状态 |
|------|------|------|
| 文件树浏览 | 侧边栏项目目录树，点击预览文件 | ⏳ 未开发 |
| 会话持久化 | 前端内存存储，刷新页面丢失 | ⏳ 等待对接后端存储 |
| 操作权限设置 | 设置面板增加"默认允许/每次确认/只读" | ⏳ 未开发 |
| ToolCallCard 组件 | 工具折叠卡片展示结果 | ⏳ 未开发 |
| Lint/Test 自动集成 | 后端有 Linter/TestRunner 工具未注册 | ⏳ 未开发 |

### 🟢 低优先级

| 功能 | 说明 | 状态 |
|------|------|------|
| 模型预设 | 预设 DeepSeek/OpenAI/火山引擎 等一键切换 | ⏳ 未开发 |
| 快捷键 | Ctrl+N 新建对话 / Ctrl+Tab 切换 / Esc 停止 | ⏳ 未开发 |
| 主题 | 代码高亮主题 / 字体调节 / 紧凑模式 | ⏳ 未开发 |

---

## 八、Git 分支与提交规范

- 分支策略：`main`（稳定）← `dev`（日常开发）
- 提交格式：`type: 描述`（如 `feat:` / `fix:` / `refactor:`）
- **前端仓库**: `ripple-desktop-Tauri` → `origin/dev`
- **后端仓库**: `pi-mono/ripple-agent` → `origin/dev`
- 每次修改代码后**必须及时提交并推送**，防止文件丢失

---

## 九、常见问题

### Q: 启动后显示"模拟"而不是"已连接"？

A: 后端未启动。检查：
1. 后端是否执行了 `node packages/server/dist/index.js`
2. 端口 3002 是否被占用（日志会看到"端口冲突"并自动清理）
3. 浏览器控制台是否有错误

### Q: 点允许后 AI 没反应？

A: 检查日志 —— 如果有 `agent_end, lastText length=0`，说明后端 API 有问题：
1. 检查 endpoint 末尾是否有逗号/空格（日志中会显示 endpoint 值）
2. 后端有清理逻辑，但仍建议手动检查配置

### Q: 点拒绝后 AI 说"创建成功"？

A: 已修复。后端拒绝路径现在发送强提示"用户已明确拒绝工具 XX，操作未执行"，AI 不会再假装成功。

### Q: 如何添加新的后端 API 调用？

A: 在 `src/services/api.ts` 中用 `request<T>()` 封装。在 `useStreamingChat` 或组件中调用。

### Q: 后端编译不过？

A: 确保在 `ripple-agent` 仓库根目录执行了 `npm install`。先编译 agent 包再编译 server 包：
```bash
cd packages/agent && npx tsc -p tsconfig.build.json
cd ../server && npx tsc -p tsconfig.build.json
```

---

## 十、对外部同事的交接要点

1. **代码在两个仓库**：前端 `ripple-desktop-Tauri`、后端 `pi-mono/ripple-agent`，都在 `dev` 分支
2. **后端必须先启动**：`node packages/server/dist/index.js`，前端才可连接
3. **DeepSeek 可用，火山引擎不可用**：火山引擎 API 响应格式兼容性问题待排查
4. **工具操作需确认**：`shell`、`write_file`、`remove` 等高风险工具需要用户确认（或开启 Auto 模式）
5. **前端不直连 LLM API**：所有 AI 请求全走后端，前端只传配置
6. **工作目录隔离**：前端传入 cwd，后端限制文件操作在此目录内
7. **每次改代码后务必 `git push**：防止文件丢失，这是血的教训（之前误删过组件文件）

---

> 完整待办清单见 `待完善清单.md`